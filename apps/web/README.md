# Web App

Next.js TypeScript scaffold for the HexRelay web client.

## Commands

- Install: `npm install`
- Run dev server: `npm run dev`
- Lint: `npm run lint`
- Test: `npm run test`
- Build: `npm run build`
