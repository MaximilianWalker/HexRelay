{
  "name": "partition-alice-bob",
  "description": "Partition Alice and Bob runtime instances from each other without enabling relay fallback.",
  "strategy": "docker",
  "supportedPlatforms": ["windows", "linux", "macos"],
  "actions": [
    {
      "type": "partition",
      "source": "alice-node",
      "target": "bob-node"
    }
  ]
}
